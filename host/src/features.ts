/**
 * Keep in sync with `src/lib/features.ts` in the wallet package.
 * Flip to true when Circle clears Arc mainnet + Onramp for production.
 * Then delete this module and all call sites.
 */
export const EnableArcMainnet = false;
